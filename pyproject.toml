[project]
name = "agente-investimento"
version = "0.0.1"
description = "Agente de Investimento usando LLMs"
authors = [
    { name = "Jeferson Dionei Sehnem" }
]
readme = "README.md"
license = { text = "MIT" }
requires-python = ">=3.10"
dependencies = [
    "bs4>=0.0.2",
    "cerebras-cloud-sdk>=1.59.0",
    "finta>=1.3",
    "groq>=0.34.1",
    "ipeadatapy>=0.1.9",
    "ipykernel>=7.1.0",
    "langchain==1.0.3",
    "langchain-community>=0.4.1",
    "langchain-core==1.0.5",
    "langchain-docling>=2.0.0",
    "langchain-google-genai>=3.0.3",
    "langchain-groq>=1.0.1",
    "langgraph==1.0.3",
    "matplotlib>=3.10.7",
    "pandas>=2.3.3",
    "pydantic-ai>=1.18.0",
    "scikit-learn>=1.7.2",
    "scipy>=1.15.3",
    "selenium>=4.36.0",
    "sidrapy>=0.1.4",
    "tabulate>=0.9.0",
    "tavily-python>=0.7.13",
    "unidecode>=1.4.0",
    "webdriver-manager>=4.0.2",
    "yfinance>=0.2.66",
]

[project.optional-dependencies]
dev = [
    "mypy==1.15.0",
    "ruff==0.9.6",
    "pytest==7.1.3",
    "pytest-cov==4.0.0",
    "black==25.1.0"
]

[build-system]
requires = ["setuptools>=77.0.0", "wheel>=0.40.0"]
build-backend = "setuptools.build_meta"

[tool.setuptools]
packages = ["agente_investimento"]
[tool.setuptools.package-dir]
"agente_investimento" = "agente_investimento"

[tool.ruff]
select = ["E", "F", "B"]
ignore = []
line-length = 88
target-version = "py310"

[tool.ruff.lint.pycodestyle]
ignore-overlong-task-comments = true
max-doc-length = 100
max-line-length = 500
#ignore-one-line-docstrings = true

[tool.pytest.ini_options]
testpaths = ["tests"]
python_files = ["test_*.py"]
addopts = "-v --cov=agente_investimento"

[tool.pyrefly]
permissive-ignores = "True"
use-untyped-imports = "True"
